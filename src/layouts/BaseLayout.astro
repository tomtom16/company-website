---
export interface Props {
  title: string;
  description?: string;
}
const { title, description = "Your trusted business partner for modern solutions." } = Astro.props;

const navLinks = [
  { href: "/", label: "Home" },
  { href: "/about", label: "About" },
  { href: "/services", label: "Services" },
  { href: "/contact-form", label: "Contact Form" },
  { href: "/contact", label: "Contact" },
];

const currentPath = Astro.url.pathname;
---

<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title} | Acme Corp</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Plus+Jakarta+Sans:wght@600;700;800&display=swap" rel="stylesheet" />
    <style is:global>
      @import '../styles/global.css';
    </style>
  </head>
  <body class="bg-cream-100 text-[#1a1a1a] font-sans antialiased">

    <!-- Navbar -->
    <header class="fixed top-0 left-0 right-0 z-50 bg-cream-100/95 backdrop-blur-md border-b border-cream-300 shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <!-- Logo -->
          <a href="/" class="flex items-center gap-2 group">
            <div class="w-8 h-8 rounded-lg bg-[#1a1a1a] flex items-center justify-center">
              <span class="text-cream-100 font-bold text-sm">A</span>
            </div>
            <span class="font-display font-bold text-xl text-[#1a1a1a] group-hover:text-brand-700 transition-colors">Acme Corp</span>
          </a>

          <!-- Desktop nav -->
          <nav class="hidden md:flex items-center gap-1">
            {navLinks.map(link => (
              <a
                href={link.href}
                class={`px-4 py-2 rounded-lg text-sm font-medium transition-all ${
                  currentPath === link.href
                    ? 'bg-brand-200 text-brand-800'
                    : 'text-gray-700 hover:text-[#1a1a1a] hover:bg-cream-200'
                }`}
              >
                {link.label}
              </a>
            ))}
            <a href="https://patient.latido.at/arzt/_Scherz" class="ml-3 px-5 py-2 bg-[#1a1a1a] hover:bg-[#2d2d2d] text-white text-sm font-semibold rounded-lg transition-colors shadow-sm">
              Terminvereinbarung
            </a>
          </nav>

          <!-- Mobile hamburger -->
          <button id="menu-toggle" class="md:hidden p-2 rounded-lg text-gray-700 hover:bg-cream-200 transition-colors" aria-label="Toggle menu">
            <svg id="icon-open" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
            </svg>
            <svg id="icon-close" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>
          </button>
        </div>
      </div>

      <!-- Mobile menu -->
      <div id="mobile-menu" class="hidden md:hidden border-t border-cream-300 bg-cream-100">
        <div class="px-4 py-3 space-y-1">
          {navLinks.map(link => (
            <a
              href={link.href}
              class={`block px-4 py-2.5 rounded-lg text-sm font-medium transition-all ${
                currentPath === link.href
                  ? 'bg-brand-200 text-brand-800'
                  : 'text-gray-700 hover:text-[#1a1a1a] hover:bg-cream-200'
              }`}
            >
              {link.label}
            </a>
          ))}
          <a href="https://patient.latido.at/arzt/_Scherz" class="block mt-2 px-4 py-2.5 bg-[#1a1a1a] hover:bg-[#2d2d2d] text-white text-sm font-semibold rounded-lg transition-colors text-center">
            Terminvereinbarung
          </a>
        </div>
      </div>
    </header>

    <!-- Page content -->
    <main class="pt-16">
      <slot />
    </main>

    <!-- Footer -->
    <footer class="bg-[#1a1a1a] text-gray-300 mt-24">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-14">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-10">
          <div class="md:col-span-2">
            <div class="flex items-center gap-2 mb-4">
              <div class="w-8 h-8 rounded-lg bg-cream-200 flex items-center justify-center">
                <span class="text-[#1a1a1a] font-bold text-sm">A</span>
              </div>
              <span class="font-display font-bold text-xl text-white">Acme Corp</span>
            </div>
            <p class="text-gray-400 text-sm leading-relaxed max-w-xs">
              Delivering modern solutions for businesses of all sizes. Quality, trust, and innovation since 2010.
            </p>
          </div>
          <div>
            <h4 class="text-white font-semibold text-sm mb-4">Navigation</h4>
            <ul class="space-y-2 text-sm">
              {navLinks.map(link => (
                <li><a href={link.href} class="hover:text-white transition-colors">{link.label}</a></li>
              ))}
            </ul>
          </div>
          <div>
            <h4 class="text-white font-semibold text-sm mb-4">Legal</h4>
            <ul class="space-y-2 text-sm">
              <li><a href="/imprint" class="hover:text-white transition-colors">Imprint</a></li>
              <li><a href="/data-protection" class="hover:text-white transition-colors">Data Protection</a></li>
            </ul>
          </div>
        </div>
        <div class="border-t border-white/10 mt-10 pt-6 text-sm text-gray-500 flex flex-col sm:flex-row justify-between gap-2">
          <span>&nbsp;</span>
          <span>© {new Date().getFullYear()} Thomas Rotter, MSc. All rights reserved.</span>
          <span>&nbsp;</span>
        </div>
      </div>
    </footer>

    <script>
      const toggle = document.getElementById('menu-toggle');
      const menu = document.getElementById('mobile-menu');
      const iconOpen = document.getElementById('icon-open');
      const iconClose = document.getElementById('icon-close');
      toggle?.addEventListener('click', () => {
        menu?.classList.toggle('hidden');
        iconOpen?.classList.toggle('hidden');
        iconClose?.classList.toggle('hidden');
      });
    </script>
  </body>
</html>
